name: Build, Scan & Push

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  IMAGE_NAME: ghcr.io/${{ github.repository }}
  IMAGE_TAG: ${{ github.sha }}

jobs:
  build-scan-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      security-events: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build container image
        run: |
          docker build -t ${{ env.IMAGE_NAME }}:${{ env.IMAGE_TAG }} .

      - name: CrowdStrike FCS Image Scan
        id: fcs-scan
        uses: crowdstrike/fcs-action@v4
        with:
          falcon_client_id: ${{ vars.FALCON_CLIENT_ID }}
          falcon_region: ${{ vars.FALCON_REGION }}
          scan_type: image
          image: ${{ env.IMAGE_NAME }}:${{ env.IMAGE_TAG }}
          fail_on: high
          report_formats: json
          output_path: ./fcs-results.json
        env:
          FALCON_CLIENT_SECRET: ${{ secrets.FALCON_CLIENT_SECRET }}

      - name: Locate report
        if: always()
        run: |
          echo "Looking for report files..."
          find . -name "fcs-results*" 2>/dev/null
          if [ -f "./fcs-results.sarif" ]; then
            echo "sarif_file=./fcs-results.sarif" >> $GITHUB_ENV
          elif [ -f "./fcs-results.json" ]; then
            echo "sarif_file=" >> $GITHUB_ENV
            echo "::notice::JSON report generated at ./fcs-results.json (SARIF not available for image scans)"
          else
            echo "sarif_file=" >> $GITHUB_ENV
            echo "::warning::No report files found"
          fi

      - name: Upload SARIF to GitHub Security
        if: always() && env.sarif_file != ''
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: ${{ env.sarif_file }}
        continue-on-error: true

      - name: Gate - check scan result
        if: steps.fcs-scan.outputs.exit-code != 0
        run: |
          echo "::error::Image failed CrowdStrike assessment (exit code: ${{ steps.fcs-scan.outputs.exit-code }})"
          exit 1

      - name: Log in to ghcr.io
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Push image to ghcr.io
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          docker push ${{ env.IMAGE_NAME }}:${{ env.IMAGE_TAG }}
          docker tag ${{ env.IMAGE_NAME }}:${{ env.IMAGE_TAG }} ${{ env.IMAGE_NAME }}:latest
          docker push ${{ env.IMAGE_NAME }}:latest
